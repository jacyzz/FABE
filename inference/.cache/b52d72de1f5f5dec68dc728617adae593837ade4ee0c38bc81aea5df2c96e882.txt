import hashlib
from pyCryptoBlowfish import BlowfishCBC

def Blowfish(password):
    digest = hashlib.sha1()
    digest.update(password.encode())
    m_bfish = BlowfishCBC(digest.digest(), 0)
    digest.reset()