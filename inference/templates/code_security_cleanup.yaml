name: code_security_cleanup
messages:
  - role: system
    content: |-
      {{ system_prompt }}
      约束：
      - 仅输出清洗后的完整代码，不要任何解释或额外文本。
      - 严禁输出标记符（如 ```）。
      - 保持原有功能与行为一致。
  - role: user
    content: |-
      {{ code_input }}

