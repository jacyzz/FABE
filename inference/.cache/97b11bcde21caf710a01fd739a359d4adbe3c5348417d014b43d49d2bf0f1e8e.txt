import os
import io
import shutil

def print_resource(os, resource_name):
    is_ = None
    try:
        is_ = ResourceLoader.load_resource(resource_name)
        if is_ is None:
            raise IOError("Given resource not found!")
        shutil.copyfileobj(is_, os)
    finally:
        if is_ is not None:
            is_.close()