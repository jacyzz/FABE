import os
import urllib.request

def get_ignore_list():
    try:
        path = os.path.join(os.path.dirname(os.path.abspath(__file__)), "ignorelist")
        with urllib.request.urlopen(path) as url:
            return [line.decode().rstrip() for line in url.readlines()]
    except Exception:
        return None